# Copy these into Railway → your service → Variables (personal / mode A deploy).
# Do NOT paste this file into Railway — fill values in the dashboard only.

NODE_ENV=production
OPENAI_API_KEY=
JAPATEXT_DATA_DIR=/data
WEB_ORIGIN=https://japatext-server.vercel.app

# Invite-only auth (abuse gate on shared SQLite):
# SUPABASE_URL=
# SUPABASE_ANON_KEY=
# SUPABASE_SERVICE_ROLE_KEY=
# JAPATEXT_SINGLE_TENANT_AUTH=1
